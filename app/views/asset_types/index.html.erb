<p style="color: green"><%= notice %></p>

<h1>Asset types</h1>

<div id="asset_types">
  <% @asset_types.each do |asset_type| %>
    <%= render asset_type %>
    <p>
      <%= link_to "Show this asset type", asset_type %>
    </p>
  <% end %>
</div>

<%= link_to "New asset type", new_asset_type_path %>
